version: '3.8'

services:
  steam-discord-bot:
    build: .
    container_name: steam-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./linked_users.json:/app/linked_users.json
    
